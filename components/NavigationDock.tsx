'use client';

import { useRouter } from 'next/navigation';
import { Home, Cake, Sparkles } from 'lucide-react';
import Dock, { type DockItemData } from './Dock';

export default function NavigationDock